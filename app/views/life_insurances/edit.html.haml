.span2
.span7
  %h1=t "editing.life_insurance"

  = render 'form'

  - if can? :read, @life_insurance
    = link_to t('shared.show'), @life_insurance, :class => 'btn'
    = link_to t('shared.back'), life_insurances_path, :class => 'btn'
  - else
    = link_to t('shared.back'), root_path, :class => 'btn'
